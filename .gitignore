# --- supplied WordPress migration material (kept on disk, never committed/deployed) ---
public_html/
localhost.sql
*.sql
*.xml
*.zip
!site/**/*.xml

# Captured WordPress HTML, kept for post-cutover comparison (5.5 MB, local only)
reference/live-snapshot/

# --- build + tooling artefacts ---
node_modules/
dist/
.astro/
.vercel/
.DS_Store
*.log

# --- secrets ---
.env
.env.*
!.env.example
